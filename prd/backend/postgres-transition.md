# Postgres Transition Tasks

- Install and enable PostGIS on PostgreSQL (local, staging, production) to support GIS/geo-spatial fields

- For GeoCoding pincodes model [ ]

  install for ubuntu
  sudo apt-get install gdal-bin

- import on logistics app models.py [ ]
  from django.contrib.gis.db import models
  from django.contrib.gis.geos import Point

-
